resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml  

namePrefix: uptimekuma-
  
labels:
  - includeSelectors: true
    includeTemplates: true
    pairs:
      app: uptimekuma
  
namespace: monitor
    
patches:
  - target:
      kind: Deployment
      name: deployment
    path: patches/diun-annotation.yaml
#  Uncomment to let traefik handle ssl certs
#  - target:
#      kind: Ingress
#      name: ingress
#    path: patches/traefik-ssl-cert.yaml
