apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mdb-config-pvc
  namespace: util
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
