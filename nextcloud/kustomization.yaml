resources:
  - deployment.yaml
  - service.yaml
  - storage/longhorn.pv.yaml
  - storage/nfs.pv.yaml
  - storage/pvc.yaml

namePrefix: nextcloud-
  
labels:
  - includeSelectors: true
    includeTemplates: true
    pairs:
      app: nextcloud
  - pairs:
      needs-media: "true"
  
namespace: org

images:
- name: nextcloud
  newName: lscr.io/linuxserver/nextcloud
  newTag: "32.0.0"
    
configMapGenerator:
  - name: map
    env: env/properties
    
patches:
  - target:
      kind: Deployment
      name: deployment
    path: patches/diun-annotation.yaml
  - target:
      kind: Deployment
      name: deployment
    path: patches/node-affinity.yaml
#  Uncomment to let traefik handle ssl certs
#  - target:
#      kind: Ingress
#      name: ingress
#    path: patches/traefik-ssl-cert.yaml

replacements:
- source:
    kind: PersistentVolume
    name: config-pv
    fieldPath: metadata.name
  targets:
  - select:
      kind: PersistentVolume
      name: config-pv
    fieldPaths:
    - spec.csi.volumeHandle
