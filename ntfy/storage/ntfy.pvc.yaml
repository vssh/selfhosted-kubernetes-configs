apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ntfy-lib-pvc
  namespace: util
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 50Mi

