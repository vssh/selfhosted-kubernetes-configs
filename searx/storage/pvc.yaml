apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc
  namespace: util
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  volumeName: pv
  resources:
    requests:
      storage: 20Mi
